def send_to_mailchimp(
    *args,
    **kwargs,
):

    """
    Connect your existing Mailchimp
    integration here.

    IMPORTANT:

    Browser preview can use:

        data:image/jpeg;base64,...

    But before sending the real email,
    upload images to:

        GCS / CDN / Mailchimp Content Studio

    and replace Base64 image sources with:

        https://...
    """

    raise NotImplementedError(
        "Mailchimp delivery "
        "is not configured."
    )